{
  "name": "portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "babel src/app.jsx -o dist/app.js && tailwindcss -i src/input.css -o dist/styles.css && cp mostafa-portfolio.html dist/index.html && cp _headers dist/ && cp -r logos dist/"
  },
  "devDependencies": {
    "@babel/cli": "^7.29.0",
    "@babel/core": "^7.29.0",
    "@babel/preset-react": "^7.29.0",
    "tailwindcss": "^3.4.0"
  }
}
